<h2 class="mb-3">ユーザー情報</h2>
<% if user_signed_in? %>
  <%= link_to user_path(user) do %>
    <%= attachment_image_tag user, :profile_image, class:"rounded-lg position-relative", fallback: "no_image.png", size: "150x188" %>
    <% if unchecked_notifications.any? %>
      <% if user == current_user %>
        <i class="fa fa-bell fa-2x position-absolute top-0 end-0 translate-middle" style="color: red;" size: "200x200"></i>
      <% end %>
    <% end %>
  <% end %>

  <p class="mt-2">名前：<%= user.name %></p>

  <% if user.prefecture2 %>
    <p>住んでいる（いた）県：<%= link_to user.prefecture2.name, posts_path(prefecture_id: user.prefecture2.id) %></p>
  <% else %>
    <p>住んでいる（いた）県：未入力</p>
  <% end %>
  <% if user.prefecture1 %>
    <p>住んでみたい県：<%= link_to user.prefecture1.name, posts_path(prefecture_id: user.prefecture1.id) %></p>
  <% else %>
    <p>住んでみたい県：未入力</p>
  <% end %>
<% else %>
  <p>まだログインしていません</p>
<% end %>

<!--sidebar・・・regionページのサイドバー-->
<!--sidebar1との違い・・・regionページには新規投稿ボタンをつけなかった-->

<%= link_to "#{prefecture.name}に関する新規投稿", new_post_path(prefecture_id: prefecture.id), class: "btn btn-info btn-block rounded-pill" %>

<%= link_to "全ての投稿を見る", all_posts_posts_path, class: "btn btn-success rounded-pill mt-3" %>

<table class="table table-striped table-hover mt-4">
  <thead>
    <tr>
      <td><h3>Region</h3></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= link_to "北海道地方", "/region/hokkaido" %></td>
    </tr>
    <tr>
      <td><%= link_to "東北地方", "/region/tohoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "関東地方", "/region/kanto" %></td>
    </tr>
    <tr>
      <td><%= link_to "中部地方", "/region/chubu" %></td>
    </tr>
    <tr>
      <td><%= link_to "近畿地方", "/region/kinki" %></td>
    </tr>
    <tr>
      <td><%= link_to "四国地方", "/region/shikoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "中国地方", "/region/chugoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "九州地方", "/region/kyusyu" %></td>
    </tr>
    <tr>
      <td><%= link_to "沖縄地方", "/region/okinawa" %></td>
    </tr>
  </tbody>
</table>