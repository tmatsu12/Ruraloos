<div class="general-visual">
  <div class="container">
    <div class="row">
      <div class="col-6">
        <h1 class="border-bottom">お問い合わせ</h1>

        <%= form_with model:@inquiry, url:inquiry_thanks_path do |f| %>

          <% if @inquiry.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <h5><strong>入力内容にエラーがあります。</strong></h5>
              <ul>
                <% @inquiry.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= form.label :name, '名前' %>
            <span class="text-danger">（必須）</span>
            <%= form.text_field :name, value: @inquiry.name, id: 'name', class: 'form-control-plaintext', :placeholder => '名前' %>
          </div>

          <div class="form-group">
            <%= form.label :email, 'メールアドレス' %>
            <span class="text-danger">（必須）</span>
            <%= form.text_field :email, value: @inquiry.email, id: 'email', class: 'form-control-plaintext', :placeholder => 'メールアドレス' %>
          </div>

          <div class="form-group">
            <%= form.label :message, 'お問い合わせ内容' %>
            <span class="text-danger">（必須）</span>
            <%= form.text_area :message, value: safe_join(@inquiry.message.split("\n"),tag(:br)), id: 'message', class: 'form-control-plaintext', :placeholder => 'お問い合わせ内容', :rows => '5' %>
          </div>

          <p>この内容で送信します。よろしいですか？</p>

          <div class="actions">
            <%= form.submit "確認", class: "btn btn-dark btn-text" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
