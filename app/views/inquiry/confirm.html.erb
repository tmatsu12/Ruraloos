<div class="general-visual">
  <div class="container">
    <div class="row">
      <div class="col-6 mx-auto">
        <h1 class="border-bottom my-3">お問い合わせ</h1>

        <%= form_with model:@inquiry, local: true, url: inquiry_thanks_path do |f| %>

          <% if @inquiry.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <h5><strong>入力内容にエラーがあります。</strong></h5>
              <ul>
                <% @inquiry.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            名前<span class="text-danger">(必須)</span>：
            <%#= f.text_field :name, value: @inquiry.name, id: 'name', class: 'form-control-plaintext', :placeholder => '名前' %>
            <%= f.hidden_field :name %>
            <%= @inquiry.name %>
          </div>

          <div class="form-group">
            メールアドレス<span class="text-danger">(必須)</span>：
            <%#= f.text_field :email, value: @inquiry.email, id: 'email', class: 'form-control-plaintext', :placeholder => 'メールアドレス' %>
            <%= f.hidden_field :email %>
            <%= @inquiry.email %>
          </div>

          <div class="form-group">
            お問い合わせ内容<span class="text-danger">(必須)</span>：<br>
            <%#= f.text_area :message, value: safe_join(@inquiry.message.split("\n"),tag(:br)), id: 'message', class: 'form-control-plaintext', :placeholder => 'お問い合わせ内容', :rows => '5' %>
            <%= f.hidden_field :message %>
            <p class="mt-3"><%= safe_join(@inquiry.message.split("\n"),tag(:br)) %></p>
          </div>

          <p class="mt-5">この内容で送信します。よろしいですか？</p>

          <div class="actions">
            <%= f.submit "送信", class: "btn btn-info btn-text mb-5 rounded-pill" %>
            <%= f.submit "戻る", name: 'back', class: "btn btn-secondary btn-text mb-5 ml-3 rounded-pill" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
